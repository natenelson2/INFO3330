{
  "name": "preishare-investor-listing-types",
  "version": "0.1.0",
  "private": true,
  "description": "Strict TypeScript types for PREIshare investor listings (Sprint 2 Topic 1) — run npm run typecheck as the pre-review gate",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
